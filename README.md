# zero run 
docker compose up -d
docker exec -it zeroclaw_container /bin/bash
ollama pull --model-of-your-choise
zeroclaw onboard
sudo apt update && sudo apt install nano -y then nano /home/linuxbrew/.zeroclaw/config.toml
change to provider to ollama then --model-of-your-choise
